# notebooks/
Colab notebooks for the heavy models (free T4 GPU). Keep one notebook per model:
recognition (VLM+OCR), generation (LLM), image-match (OpenCLIP). Export reusable code back
into `src/snap_to_sell/`.
