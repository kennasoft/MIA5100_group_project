# data/

- `testset/` — 50 phone photos + `labels.csv` (ground-truth identity + observed price). - `catalogue.csv`, `feedback.csv` — written at runtime by the pipeline.
Include tobacco items in the test set for the age-restriction guardrail evaluation.
